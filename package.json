{
  "name": "s-escapes-website",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/build.mjs",
    "build:site": "vite build",
    "build:cms": "tinacms build",
    "verify": "npm run build:site && node --check netlify/functions/leads.mjs && node --check netlify/functions/reviews.mjs && node --check netlify/functions/lib/shared.mjs",
    "preview": "vite preview",
    "dev:netlify": "npx netlify-cli dev"
  },
  "devDependencies": {
    "@netlify/functions": "^3.1.10",
    "@tinacms/cli": "^2.1.0",
    "tinacms": "^2.9.0",
    "vite": "^7.0.0"
  }
}
